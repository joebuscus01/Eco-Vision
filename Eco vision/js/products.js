function storeproduct(name, price, img, fit){

    //     localStorage.clear()
    localStorage.setItem("name", name)
    localStorage.setItem("price", price)
    localStorage.setItem("img", img)
    localStorage.setItem("fit", fit)
}